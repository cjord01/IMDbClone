<div class="row">
  <div class="large-4 columns">
		<%= form_for @movie do |f| %>
			<%= f.text_field :title, placeholder: "Title" %>
			<%= f.submit "Add Movie", :class=>"button"%>
		<% end %>
	</div>
</div>